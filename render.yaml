services:
  - type: web
    name: abaddon
    env: node
    buildCommand: |
      npm install
      apt-get update
      apt-get install -y ffmpeg
      pip install yt-dlp
    startCommand: node server.js